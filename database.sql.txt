-- SmartCart E-Commerce Database

CREATE DATABASE IF NOT EXISTS smartcart;
USE smartcart;

-- Users table
CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(100),
    password VARCHAR(100)
);

-- Products table
CREATE TABLE products (
    product_id INT AUTO_INCREMENT PRIMARY KEY,
    product_name VARCHAR(100),
    price INT,
    image_url VARCHAR(255)
);

-- Cart table
CREATE TABLE cart (
    cart_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT,
    product_id INT,
    quantity INT,
    FOREIGN KEY (user_id) REFERENCES users(user_id),
    FOREIGN KEY (product_id) REFERENCES products(product_id)
);

-- Sample product data
INSERT INTO products (product_name, price, image_url) VALUES
('Smart Watch', 1999, 'watch.jpg'),
('Wireless Earbuds', 1499, 'earbuds.jpg'),
('Backpack', 999, 'bag.jpg');